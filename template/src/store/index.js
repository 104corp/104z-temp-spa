import Vue from 'vue'
import Vuex from 'vuex'
import userAgent from './userAgent'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    userAgent
  }
})
